VITE_API_URL=http://localhost:8000
# Production backend (already the built-in fallback in src/api/client.js,
# but set this explicitly in Vercel's dashboard too so it's not implicit):
# VITE_API_URL=https://4saesonbackend.vercel.app
