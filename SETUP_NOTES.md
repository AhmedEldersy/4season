# 4Seasons — تشغيل المشروع بعد التعديل

## الباك إند (FastAPI)

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate        # على ويندوز: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env              # وحدث القيم لو محتاج (Google OAuth، SECRET_KEY، ...)
python -m app.seed                # ينشئ قاعدة البيانات + حساب الأدمن + مناطق التوصيل + بيانات تجريبية
uvicorn app.main:app --reload --port 8000
```

بعد `seed.py` هيطبع لك بيانات دخول الأدمن (owner@4season.com / Admin@12345 كافتراضي — غيّرها).

## الفرونت إند (React/Vite)

```bash
cd frontend
npm install
cp .env.example .env               # VITE_API_URL=http://localhost:8000
npm run dev                        # للتطوير
npm run build                      # للنشر
```

## أهم التعديلات اللي تمت (تفاصيل كاملة في الشات)

- **ميزة استلام من الفرع / توصيل** كاملة: موديل `DeliveryArea` جديد، الأدمن بيتحكم في المناطق والرسوم بالكامل من `/admin/delivery-areas`، والباك إند هو اللي بيحسب الرسوم والإجمالي دايمًا (الفرونت مش مصدر الحقيقة).
- **مناطق التوصيل بقت فاضية بشكل مقصود** (شلنا مناطق القاهرة التجريبية لأنها مش حقيقية) — الأدمن يضيف مناطقه الحقيقية من الصفحة.
- **الرسوم القديمة محفوظة تاريخيًا**: لو الأدمن غيّر رسم منطقة، الطلبات القديمة تفضل بالرسم الأصلي وقت الطلب.
- **إصلاح Race condition** في توليد رقم الطلب (كان MAX()+1 غير آمن مع الطلبات المتزامنة).
- **حماية من تكرار الطلب** عند الضغط على "تأكيد الطلب" أكتر من مرة (idempotency key).
- **لوجو 4Seasons الحقيقي** متركب بخلفية شفافة في الـ Navbar، الفوتر، صفحات تسجيل الدخول، لوحة تحكم الأدمن، الصفحة الرئيسية، والفافيكون.
- صفحة أدمن جديدة **"مناطق التوصيل"** (إضافة/تعديل/تفعيل/تعطيل/حذف مع كل الفاليديشن).
- تتبع الطلب بقى يفرّق بين استلام وتوصيل (مفيش "خرج للتوصيل" لطلبات الاستلام).
- **صفحة التحليلات (Analytics) بقت كاملة**: إيرادات، أفضل منتجات، أفضل عملاء إنفاقًا، ساعات/أيام الذروة، تحليل الإلغاءات، توصيل مقابل استلام، الإيرادات حسب منطقة التوصيل، وطرق الدفع.
- **إصلاح باج أمان**: حذف صنف (Category) فيه منتجات كان ممكن يكسر المنيو بدون تحذير — دلوقتي بيرفض ويطلب تعطيل الصنف بدل حذفه.
- **إصلاح ثغرة رفع الصور**: كان بيقبل أي ملف بأي امتداد بدون فحص — دلوقتي بيتحقق من نوع الملف الحقيقي (JPG/PNG/WEBP فقط، فحص المحتوى الفعلي مش بس الاسم) وحجمه (حد أقصى 5MB).
- تم اختبار الفلو كامل (تسجيل → قائمة → سلة → استلام/توصيل → تتبع الطلب → لوحة الأدمن → إضافة منطقة توصيل → التحليلات) ببراوزر حقيقي (Playwright)، واكتشفنا وصححنا باجات حقيقية بدل الافتراض إن كل حاجة شغالة.

## لسه ناقص (مذكور بالتفصيل في الشات)

- إعادة التصميم الكاملة بالأسود والأبيض (الموقع دلوقتي بألوان حمرا/كريمي).
- اختبار حقيقي لتسجيل الدخول بجوجل (يحتاج مفاتيح Google OAuth حقيقية).
- Rate limiting، اختبارات آلية (automated tests)، فحص أمني شامل، تقرير QA نهائي.

## آخر إصلاحات (مهم جدًا تقراها)

- **باج خطير كان بيكسر صفحة "الأصناف" في الأدمن بالكامل** (بيرجع صفحة فاضية) — كان فيه سطر كود غلط في React خلى الصفحة تعمل crash صامت. تم إصلاحه واختباره فعليًا.
- **تسجيل الدخول بحساب الأدمن من صفحة العميل العادية** (`/login`) دلوقتي بيوجهك تلقائيًا لصفحة الأدمن (`/admin`) — مش لازم تكتب الرابط بنفسك.
- **مفيش داتا وهمية تلقائية بقى** — `python -m app.seed` دلوقتي بيعمل بس: حساب الأدمن + المنيو + الأصناف، وبيسيب مناطق التوصيل والطلبات والعملاء فاضية عشان تجرب بنفسك. (لو عايز الداتا التجريبية القديمة للتحليلات، شغّل: `python -m app.seed --with-demo-data`)
- ضفت "Error Boundary" في لوحة الأدمن — لو حصل خطأ غير متوقع في أي صفحة تانية، هيظهرلك رسالة واضحة وزرار "إعادة المحاولة" بدل صفحة فاضية بدون تفسير.
- تأكدت فعليًا (مش افتراض) إن أي تعديل في الأدمن (إضافة صنف، منتج، منطقة توصيل) بيظهر فورًا في صفحات العميل — جربت ده لايف بمتصفح حقيقي.
- **إجبار تسجيل الدخول بجوجل على استكمال البيانات**: أي حساب (حتى لو دخل بجوجل) لازم يكون عنده رقم موبايل محفوظ قبل ما يقدر يعمل أي حاجة (Checkout، الطلبات، البروفايل). لو ناقص، بيتحول تلقائيًا لصفحة "كمّل بياناتك" ومش يقدر يتجاوزها. البيانات بتتحفظ دايمًا على حسابه (`PATCH /auth/me`)، مش سؤال بيتكرر كل مرة. والتحقق ده موجود في الباك إند نفسه كمان (مش بس الواجهة) — يعني حتى لو حد حاول ينده على الـ API مباشرة بدون رقم موبايل، هيترفض بخطأ 403.
- **رقم الموبايل بقى لازم يكون 11 رقم بالضبط ويبدأ بـ 01** (رقم مصري قياسي) — في التسجيل، وفي "كمّل بياناتك"، وفي الـ Checkout. الفرونت إند بيرفض أي حرف غير رقمي وقت الكتابة (ومايسمحش بأكتر من 11 رقم)، والباك إند برفض أي رقم غير مطابق للنموذج حتى لو حد حاول يتخطى الواجهة وينده على الـ API مباشرة.
- **باج خطير جدًا كان بيعمل صفحة بيضاء بعد أي خطأ فاليديشن** (تسجيل، تسجيل دخول، تشيك أوت، إدارة الأصناف/المناطق/الطلبات في الأدمن — 9 صفحات فعليًا). السبب: رسائل خطأ الباك إند بترجع كـ"مصفوفة" في حالات معيّنة (زي رقم موبايل غلط) لا كنص عادي، والكود القديم كان بيحاول يعرضها كنص فيعمل React crash فوري بدون أي رسالة. اتصلح بمعالج واحد مركزي (`getErrorMessage`) بيتأكد دايمًا إنه يعرض نص مفهوم، مش يكسر الصفحة. جربته لايف وأكدت إن الرسالة بقت تظهر صح ("رقم الموبايل لازم يكون 11 رقم ويبدأ بـ 01") من غير أي كراش.

## ملحوظة مهمة

بعد فك الضغط، لازم تحذف أي `4season.db` قديم عندك، وتشغّل `python -m app.seed` تاني عشان قاعدة البيانات تتعمل من جديد بالموديلات والإصلاحات المحدثة.

## دعم الرفع على Vercel بالكامل (فرونت + باك)

المشروع دلوقتي جاهز يترفع بالكامل على Vercel (فرونت إند + باك إند)، مش بس الفرونت إند:

- `backend/api/index.py` — ملف دخول (entrypoint) خاص بـ Vercel بيصدّر تطبيق FastAPI كـ Serverless Function. الكود الأساسي في `app/` متأثر بالمرة.
- `backend/vercel.json` — بيوجّه كل الطلبات لملف الدخول ده.
- الصور المرفوعة من الأدمن هتتخزن في `/tmp` تلقائيًا لو شغال على Vercel (بيتضبط لوحده)، بس **ده تخزين مؤقت غير دائم** — الصور ممكن تضيع بين الطلبات المختلفة. لو عايز الصور تفضل موجودة بشكل دائم، الحل الصحيح إنك تستخدم Cloudinary أو S3 بدل الرفع المحلي (مش متوفر في النسخة دي بعد).
- **الإشعارات اللحظية (WebSocket) للأدمن وتتبع الطلب للعميل بقى ليها نسخة احتياطية تلقائية (Polling)** — لو WebSocket ما اتوصلش في أول 4 ثواني أو قفل، النظام يبدأ يسأل السيرفر كل 6-8 ثواني تلقائيًا بدل ما يفضل ساكت. ده معناه المشروع يشتغل صحيح على Vercel (اللي مش بيدعم WebSocket دايم) بدون ما تخسر الميزة، بس هتكون أبطأ شوية (تأخير لحد 8 ثواني) بدل فورية. جربت السيناريو ده لايف بمتصفح حقيقي (قفلت WebSocket تمامًا) وأكدت إن الطلب الجديد ظهر في لوحة الأدمن.
- خطوات الرفع: اعمل مشروعين منفصلين على Vercel (واحد Root Directory = `backend`، والتاني Root Directory = `frontend`)، ونفس متغيرات البيئة اللي اتكلمنا عليها قبل كده (`DATABASE_URL`, `SECRET_KEY`, `FRONTEND_URL`, `BACKEND_URL`, `VITE_API_URL`...).
